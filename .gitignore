# the engine clone scripts/build.sh makes, and its target dir
.synsema/
# Horizen's starter kit clone scripts/devnet.sh makes
.vela-devnet/
# built modules
build/
# keys, addresses and tokens: every client/.env* except the example
client/.env*
!client/.env.example
# real payroll data: every CSV in client/ except the example
client/*.csv
!client/payroll.example.csv
# the console's state and the guest it cached (a volume on the platform); its .env at the root
/data/
/.env*
!/.env.example
